/**
 * Drizzle schema barrel — implementation of docs/architecture/02-database-schema.md.
 * Prefer importing from `@/lib/db/schema` (this file) so drizzle-kit and app stay aligned.
 */
export * from "./schema/index";
