# Local secrets — do not commit. Template: .env.example
# Keep keys in sync with .env.example so every developer is aligned.

# -----------------------------------------------------------------------------
# Database [required]
# -----------------------------------------------------------------------------
# Local fallback (unused while Neon is set):
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app_db
DATABASE_URL=postgresql://neondb_owner:npg_vnKtlz30kspX@ep-hidden-rain-atzivntf-pooler.c-9.us-east-1.aws.neon.tech/neondb?channel_binding=require&sslmode=require

# -----------------------------------------------------------------------------
# App URL
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://127.0.0.1:3000

# -----------------------------------------------------------------------------
# Better Auth [auth] — values ready; wiring deferred
# -----------------------------------------------------------------------------
BETTER_AUTH_SECRET=H2IYZ6nFzmUsRdKTr35IICthzUiE8udHxFWOYp2LJ44=
BETTER_AUTH_URL=http://127.0.0.1:3000
BETTER_AUTH_TRUSTED_ORIGINS=http://127.0.0.1:3000,http://localhost:3000

# -----------------------------------------------------------------------------
# Bootstrap / seed users [seed]
# -----------------------------------------------------------------------------
AUTH_ADMIN_EMAIL=admin@example.com
AUTH_ADMIN_PASSWORD=ChangeMe123!
AUTH_ADMIN_NAME=Project Admin
AUTH_ADMIN_USERNAME=admin
AUTH_ADMIN_PHONE_NUMBER=0000000000

# -----------------------------------------------------------------------------
# Document uploads [upload] — ADR-040
# -----------------------------------------------------------------------------
UPLOAD_MAX_BYTES=5242880
UPLOAD_ALLOWED_MIME=*
# UPLOAD_STORAGE_DIR=uploads

# -----------------------------------------------------------------------------
# Playwright E2E [e2e]
# -----------------------------------------------------------------------------
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001
PLAYWRIGHT_ADMIN_EMAIL=admin@example.com
PLAYWRIGHT_ADMIN_USERNAME=admin
PLAYWRIGHT_ADMIN_PASSWORD=ChangeMe123!
